program wwwMail;

{$APPTYPE CONSOLE}

uses
  WebBroker,
  CGIApp_jam,
  udmMail in 'udmMail.pas' {dmMail: TWebModule};

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TdmMail, dmMail);
  Application.Run;
end.
