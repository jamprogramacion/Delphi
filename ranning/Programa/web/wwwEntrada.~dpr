program wwwEntrada;

{$APPTYPE CONSOLE}

uses
  WebBroker,
  CGIApp_jam,
  udmEntrada in 'udmEntrada.pas' {dmEntrada: TWebModule},
  uComun in 'uComun.pas';

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TdmEntrada, dmEntrada);
  Application.Run;
end.
